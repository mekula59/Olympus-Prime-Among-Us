export interface HeroMetric {
  label: string;
  value: string;
  detail: string;
}

export interface MemoryBeat {
  title: string;
  copy: string;
  note: string;
}

export interface RitualStop {
  slot: string;
  title: string;
  detail: string;
}

export interface CrewMood {
  title: string;
  copy: string;
  badge: string;
}

export interface TransmissionNote {
  title: string;
  detail: string;
}
